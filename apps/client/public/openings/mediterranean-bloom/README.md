# Mediterranean Bloom media

Original generic artwork generated on 2026-09-08 through Replicate:

- First-frame image: `google/nano-banana-2-lite`.
- Silent image-to-video animation: `bytedance/seedance-2.5`, requested duration 5 seconds, 720p.
- `opening.mp4`: H.264, yuv420p, no audio, with the MP4 index moved to the front for streaming.
- `poster.jpg`: the opening video's actual first frame.

Art direction follows the site's Mediterranean palette: cream `#fdf9ec`, blue `#1d4f91`,
lemon `#f2c53d`, and green `#4f7a3c`. Shading and generated materials vary around those colours.
The artwork contains no names, initials, dates, venue details, or guest information.
Personalised lettering is rendered by `MediterraneanArt` at runtime.

The portrait is intentionally framed on wide screens; avoid a full-width video crop that cuts
away the seal. If replacing the video, inspect the seal centre and the final card's position
against the live overlays, and extract a new matching poster.
